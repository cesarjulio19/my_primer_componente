import { NombreAbreviadoPipe } from './nombre-abreviado.pipe';

describe('NombreAbreviadoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreAbreviadoPipe();
    expect(pipe).toBeTruthy();
  });
});
